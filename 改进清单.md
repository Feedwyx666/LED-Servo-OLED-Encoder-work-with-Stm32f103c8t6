# 代码改进清单

通读全部自写源码后整理，按「改动小且收益确定」排序。

**这份清单只写问题在哪、为什么是问题、往哪个方向改，不给可直接粘贴的完整代码**——你说了要自己练手，留着自己写。改完可以让我复核。

清单里的行号对应 commit `0850a42` 时的代码，改动过程中行号会漂移，以函数名定位为准。

---

## 第一优先：几行就能改完，收益确定

### 1. 两个跨中断共享的变量缺 `volatile`

| 变量 | 位置 | 谁写 | 谁读 |
|------|------|------|------|
| `Encoder_Count` | `Encoder.c:89` | EXTI0/EXTI1 中断 | 主循环 |
| `count` | `KeyEXTI.c:8` | EXTI15_10 中断 | 主循环 `return_count()` |

全工程 `Hardware/ System/ User/` 里一个 `volatile` 都没有。

**为什么是问题：** 编译器做优化时只看当前函数的控制流。它看到主循环里反复读一个全局变量、而循环体内没有任何代码写它，就有权认为这个值不会变，于是把它读进寄存器缓存起来，后续直接用寄存器——中断改了内存里的值，主循环却读不到，画面卡死在旧值。`volatile` 的作用就是告诉编译器「这个变量随时可能被你看不见的代码改掉，每次用都必须重新从内存读」。

**现在没炸的原因：** 你的优化等级低，而且 `GetEncoder_Count()` 和 `Encoder_Count` 在同一个 .c 里但调用点在 main.c，跨编译单元的函数调用会强制编译器假设内存可能被改动。一旦开 `-O2` 或启用跨模块优化（LTO），行为就可能变。

**方向：** 定义处加 `volatile` 限定符。这是本清单里最该改的一条，改动量是两个关键字。

---

### 2. 主循环一轮里取了 4 次编码器值，可能取到不同的数

`main.c` 第 50、54、59、60 行各调一次 `GetEncoder_Count()`：

```
50 行  OLED 显示 LED 百分比
54 行  OLED 显示舵机角度（×1.8）
59 行  写 TIM2 CCR1（LED 占空比）
60 行  写舵机角度
```

**为什么是问题：** 中断可以在任意两次调用之间插进来改掉计数。于是同一轮循环里，OLED 上显示的百分比、显示的角度、实际写进 CCR 的值，可能来自三个不同时刻的计数值。表现是显示和实际输出对不上，转得快时尤其明显。

**方向：** 循环开头用一个局部变量取一次快照，本轮全部用这个局部变量。局部变量在栈上、只有主循环能碰，天然没有竞态。

---

### 3. `Servo_SetAngel()` 没有入参限幅（唯一会烧硬件的一条）

`Servo.c:7`

```c
TIM_SetCompare2(TIM2, Angel / 180 * 2000 + 500);
```

**为什么是问题：** 传进 <0 或 >180 的角度，算出的 CCR 会跑到 500 以下或 2500 以上，对应脉宽超出舵机的有效行程。舵机会一直顶向机械限位，**堵转**。舵机堵转电流是空载的好几倍，持续堵转会发热、烧线圈、烧驱动板。这是清单里唯一可能造成物理损坏的问题。

现在靠 `GetEncoder_Count()` 限幅在 0~100、乘 1.8 得 0~180，间接安全。但 `Servo_SetAngel` 是对外的公共接口，安全性不该依赖调用方的自觉——你以后从别处调它，或者改了编码器的限幅范围，这个保护就没了。

**方向：** 函数入口自己夹住角度范围。防御要放在被调用方，这是接口设计的通则。

**顺带：** `Angel` 是「天使」，角度是 `Angle`。函数名和参数名都拼错了。不影响运行，但建议一起改掉，`Servo.h` 的声明也要同步。

---

### 4. 舵机通道初始 CCR = 0，上电瞬间发的不是有效舵机信号

`PWM.c:78` 把 `TIM_Pulse = 0`，第 87、88 行 `TIM_OC1Init` 和 `TIM_OC2Init` **复用同一个** `TIM_OCInitStruct`，所以 CH2（舵机）的初始 CCR 也是 0。

**为什么是问题：** 舵机的有效脉宽是 0.5~2.5 ms（CCR 500~2500）。CCR=0 意味着输出恒低，这不是舵机协议里的任何合法值。从上电到主循环第一次调 `Servo_SetAngel` 之间，舵机收到的是无效信号，可能抖动、无力、或停在上次断电的位置。

CH1（LED）初始为 0 是对的——占空比 0% 就是灭，符合预期。问题只在 CH2。

**方向：** 两个通道的初始值需求不同，就不该共用一个初始化结构体。给 OC2 单独设一个中位脉宽（90° 对应 CCR 1500）再调 `TIM_OC2Init`。

---

## 第二优先：不规范但当前不会咬你

### 5. `GetEncoder_Count()` 是有副作用的 getter，且限幅有竞态

`Encoder.c:91-100`

```c
int GetEncoder_Count(void)
{
    if (Encoder_Count > 100) Encoder_Count = 100;   // 在 get 里改全局变量
    if (Encoder_Count < 0)   Encoder_Count = 0;
    return Encoder_Count;
}
```

**两个问题：**

**副作用** — 名字叫 `Get...` 的函数偷偷修改全局状态，违反命名预期。读代码的人不会想到调一次 getter 会改掉数据。

**读-改-写竞态** — `if (Encoder_Count > 100)` 判断、然后 `Encoder_Count = 100` 写回，这个序列不是原子的。如果判断完、写回前来了一次 EXTI 中断执行 `Encoder_Count++`，那次递增会被紧随的写回覆盖，丢一个计数。手转编码器频率低，撞上的概率极小，但机理成立。

**方向：** 限幅应该放在**产生数据的地方**，也就是 EXTI0/EXTI1 中断里递增/递减之后就地夹住。这样 getter 变成纯读取，副作用和竞态一起消失。这条和第 1、2 条是同一个主题（共享状态的管理），可以一起改。

---

### 6. `cnt * 200` 在满量程时超出 ARR

`main.c:59`

```c
TIM_SetCompare1(TIM2, GetEncoder_Count() * 200);
```

ARR = 20000-1 = 19999，`cnt` 范围 0~100，所以 `cnt*200` 范围 0~20000。`cnt=100` 时 CCR=20000 > ARR=19999。

**为什么是问题：** CCR > ARR 时，计数器从 0 数到 ARR 溢出的整个周期内都不会发生比较匹配，输出保持初始有效电平不变。PWM1 模式 + 极性 High 的情况下就是恒高，**效果上确实是 100% 亮，看起来正常**。但寄存器语义上是超量程的，属于「靠越界得到了正确结果」，换个模式或极性就不一定还对。

**方向：** 占空比换算应该按 `cnt * (ARR+1) / 100` 的思路算，并确保结果不超过 ARR。注意整数运算的顺序会影响精度，先乘后除。

**注：** 我口头说过改成 `cnt * 199`，那是错的——那样 `cnt=100` 只有 99.5%，等于用一个新误差盖掉旧问题。别按这个改。

---

### 7. `GetEncoder_Count()` 声明与定义类型不一致

`Encoder.h:4` 声明返回 `int16_t`，`Encoder.c:91` 定义返回 `int`。

**为什么编译器没报错：** `Encoder.c` 只 include 了 `stm32f10x.h`、`LED.h`、`Delay.h`，**没有 include 自己的 `Encoder.h`**，所以这个编译单元里看不到声明，两者对不上也无从发现。

**实际影响：** 比我口头说的小。ARM ABI 上返回值都在 r0，`int16_t` 让调用方做一次符号扩展截断到 16 位，而你的值域 0~100 远在 `int16_t` 范围内，**任何情况下都不会被截断影响**。我之前把这条排第一、说成「埋着的雷」，是判断失误。

**方向：** 真正的正解不是手工比对两处类型，而是**让 `Encoder.c` include 自己的 `Encoder.h`**。加上之后编译器立刻报类型冲突，逼你统一，而且以后任何声明/定义不一致都会被自动抓到。这是 C 工程的一个通用惯例：每个 .c 都应该 include 自己的 .h，让编译器帮你校验对外接口。

---

### 8. `OLED_ShowNum` 收 `uint32_t`，传浮点会截断

`main.c:54`

```c
OLED_ShowNum(3, 6, GetEncoder_Count() * 1.8, 3);
```

`OLED.h:16` 的第三个参数类型是 `uint32_t`。`cnt*1.8` 是浮点，隐式转换成整数时是**向零截断**，不是四舍五入。`cnt=1` 时 1.8° 会显示成 1。

**方向：** 如果丢小数是你接受的行为，那不用改，只要知道有这回事。想显示一位小数，得自己把整数部分和小数部分分开显示；想四舍五入，转换前先加 0.5。

顺带：M3 没有硬件浮点单元，`*1.8` 走的是软件浮点库，每次循环都算两遍（54 行和 60 行各一次）。用整数运算代替（比如 `cnt*9/5`）会快很多，也顺便解决截断的不确定性。

---

## 第三优先：结构性改动，想清楚再动

### 9. LED 和舵机共用 TIM2，被迫都跑 50 Hz

**这是 PWM 的一个硬约束，值得记住：** 同一个定时器的不同通道，占空比可以各自独立（各有自己的 CCR），但**频率必须相同**（共用 PSC 和 ARR）。

当前 `PSC=72-1`、`ARR=20000-1` → 计数时钟 1 MHz、周期 20 ms、频率 50 Hz。这套参数是**为舵机选的**，SG90 这类舵机要求 20 ms 周期，没得商量。好处是 CCR 的单位刚好是 μs，角度换算不用乘系数。

代价是 LED 也只能 50 Hz。人眼的闪烁融合频率大约 60~90 Hz，50 Hz 调光在低占空比时可能看得见闪烁，余光扫过时更容易察觉。LED 调光一般用 500 Hz~1 kHz 以上。

**动手前先确认：你实际看得见闪烁吗？** 高占空比时不明显，低占空比和余光扫过时才容易察觉。如果肉眼没感觉，这条可以先不动——它是本清单唯一需要改定时器分配和换引脚的改动，成本最高。

**方向（如果要改）：** 把 LED 挪到另一个定时器，比如 TIM3_CH1 = PA6，或 TIM4。新定时器按 LED 需求单独配 PSC/ARR（1 kHz 左右），舵机留在 TIM2 不动。注意换引脚要改接线。

---

### 10. 按键消抖在中断里阻塞

`KeyEXTI.c` 的 `EXTI15_10_IRQHandler` 和 `Encoder.c` 的 `EXTI3_IRQHandler` 都是这个写法：

```c
EXTI->IMR &= ~EXTI_LineX;   // 屏蔽本线，防抖动期重入
Delay_ms(20);               // 跳过机械抖动
if (读回引脚仍为低) {
    ...
    while (引脚仍为低) {}   // 等释放，无上限
    Delay_ms(20);
}
EXTI_ClearITPendingBit(EXTI_LineX);
EXTI->IMR |= EXTI_LineX;    // 恢复
```

先说**做对的地方**：只操作 `EXTI->IMR` 而不重新调 `EXTI_Init()`。后者会连边沿配置一起重写，容易把触发方式改掉；直接改中断屏蔽寄存器只动该动的位。这个写法是对的，保持。

`Delay_ms` 走的是 SysTick 轮询（`Delay.c:18-27`，`while` 等 COUNTFLAG，不开中断），所以在中断里调它**不会死锁**，能正常工作。

**真正的代价：** 中断里阻塞了几十毫秒，按住不放则无限等待。这期间主循环停摆，OLED 不刷新、CCR 不更新。

**比这更实际的一个问题：** EXTI3（编码器按键）和 EXTI0/EXTI1（编码器 A/B 相）的**抢占优先级都是 2**（`Encoder.c` 第 40、47、79 行）。同级中断不能互相抢占——**按住编码器按键不放的那段时间，转动编码器的计数会全部丢失**。

**方向：** 正规做法是中断只置一个标志位就立刻返回，消抖交给主循环里的状态机做（定时扫描引脚电平，连续 N 次读到同一状态才认）。这是个值得练的模式，但改动比前面几条大。

短期缓解也可以只调整优先级，让编码器 A/B 相的抢占优先级高于按键，这样按键阻塞时编码器仍能计数。

---

## 清理项：不影响功能

- **`Encoder.c` 第 146、171 行注释写错**：写着「屏蔽 EXTI14 中断」「恢复 EXTI14 中断」，实际操作的是 `EXTI_Line3`。从 `KeyEXTI.c` 复制过来忘改。
- **`NVIC_PriorityGroupConfig` 调了三次**：`KeyEXTI.c:39`、`Encoder.c:35`、`Encoder.c:74`。值都是 `NVIC_PriorityGroup_2` 所以无害，但优先级分组是**全芯片唯一**的配置，规范做法是在 `main()` 开头设一次。分散在各模块里的风险是：哪天某个模块改了分组值，会静默影响其他所有中断的优先级解释方式。
- **`ClockInit()` 定义了但全工程无人调用**：`ClockInit.c:8`。只有 `LED.c:2` include 了头文件，没有任何调用点。各模块都自己开时钟，这个文件是冗余的，可以删。
- **`PWM.h` 只暴露了 `PWM_Init`，封装漏了**：`main.c:59` 直接调库函数 `TIM_SetCompare1(TIM2, ...)`，绕过了 PWM 模块。正常做法是 `PWM.h` 提供 `PWM_SetCompare1(uint16_t)` 之类的接口，main 不直接碰具体定时器。同理 `Servo.c` include 了 `PWM.h` 但一行也没用到（它调的是库函数 `TIM_SetCompare2`）。这不算 bug，是分层习惯。

---

## 建议的动手顺序

```
1  →  volatile（两个关键字）
2  →  主循环取快照（一个局部变量）
3  →  舵机入参限幅（几行 if）+ Angel 改 Angle
4  →  OC2 初值给 1500
5  →  限幅挪进中断，getter 变纯读
6  →  cnt*200 的换算
7  →  Encoder.c 补 include 自己的头文件，然后统一类型
清理项  →  注释、NVIC 分组挪到 main、删 ClockInit
9/10 →  最后再看，都是结构性改动
```

前四条都是几行以内的改动，做完再编译烧录验证一遍，确认功能没退化，然后往下走。

编译烧录命令（Git Bash，Keil 路径按本机为准）：

```bash
"/c/Keil_v5/UV4/UV4.exe" -b "Project.uvprojx" -j0 -o "build_log.txt"   # 期待 0 Error(s), 0 Warning(s)
"/c/Keil_v5/UV4/UV4.exe" -f "Project.uvprojx" -o "flash_log.txt"       # 期待 Verify OK. Application running
```
