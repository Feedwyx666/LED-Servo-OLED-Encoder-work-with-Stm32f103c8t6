# STM32 编码器调节 PWM 工程问题清单

生成时间：2026-08-13  
状态：功能已正常工作，以下是潜在问题与优化建议

---

## 一、需要修复的问题（按优先级排序）

### 1. 缺少 volatile 关键字（高优先级）

**位置：** `Encoder.c:89`

**当前代码：**
```c
int16_t Encoder_Count = 0;
```

**问题：**
- `Encoder_Count` 在中断里修改（`EXTI0_IRQHandler`、`EXTI1_IRQHandler`），主循环里读取
- 编译器不知道中断会改它，可能把它缓存在寄存器里，导致主循环读到的永远是旧值
- 现在能跑是因为优化等级低 + 跨文件调用，但开 `-O2` 或 LTO 后可能失效

**修复方法：**
```c
volatile int16_t Encoder_Count = 0;
```

**原理：**
`volatile` 告诉编译器"这个变量随时可能被看不见的代码改掉，每次用到它都必须重新从内存读"。所有中断共享变量、硬件寄存器都需要加。

---

### 2. LED PWM 占空比计算越界（中优先级）

**位置：** `main.c:64`

**当前代码：**
```c
TIM_SetCompare1(TIM2, NowEncoder*200);
```

**问题：**
- `ARR = 19999`，CCR 合法范围是 `0~19999`
- 当 `NowEncoder=100` 时，`CCR=20000 > ARR`
- CCR ≥ ARR+1 时，PWM1 模式下输出恒高（100% 亮），寄存器语义上越界了

**修复方法：**
```c
TIM_SetCompare1(TIM2, NowEncoder*199);  // 或 NowEncoder*200-1
```

**效果：**
- `NowEncoder=0` → CCR=0 → 0% 占空比
- `NowEncoder=100` → CCR=19900 → 99.5% 占空比（足够接近 100%）

---

### 3. CheckEncoderCount() 有竞态风险（低优先级）

**位置：** `Encoder.c:97-103`

**当前代码：**
```c
void CheckEncoderCount()
{
    if (Encoder_Count>100)
        Encoder_Count = 100;
    if (Encoder_Count<0)
        Encoder_Count = 0;
}
```

**问题：**
- "读-判断-写"不是原子操作
- 如果在 `if (Encoder_Count>100)` 判断完、`Encoder_Count = 100` 写回之前，另一个中断改了它，那次改动会被覆盖
- 现在只有两个中断（一个++、一个--），且不会同时触发，所以实际安全
- 但这是个隐患，如果以后加第三个中断修改这个变量，会出问题

**修复方法：**
直接在中断里递增/递减后立刻限幅，不要单独拆成函数：

```c
void EXTI0_IRQHandler(void)
{
    if(EXTI_GetITStatus(EXTI_Line0)==SET)
    {
        if(GPIO_ReadInputDataBit(GPIOB,GPIO_Pin_1)==0)
        {
            Encoder_Count--;
            if (Encoder_Count < 0) Encoder_Count = 0;  // 就地限幅
        }
        EXTI_ClearITPendingBit(EXTI_Line0);
    }
}

void EXTI1_IRQHandler(void)
{
    if(EXTI_GetITStatus(EXTI_Line1)==SET)
    {
        if(GPIO_ReadInputDataBit(GPIOB,GPIO_Pin_0)==0)
        {
            Encoder_Count++;
            if (Encoder_Count > 100) Encoder_Count = 100;  // 就地限幅
        }
        EXTI_ClearITPendingBit(EXTI_Line1);
    }
}
```

---

## 二、体验优化建议（不影响功能）

### 4. LED PWM 频率太低，会闪烁

**问题：**
- LED 和舵机共用 TIM2，频率都是 50Hz
- 人眼闪烁融合频率约 60~90Hz，50Hz 在低占空比时会肉眼可见闪烁（尤其余光扫过时）
- 舵机必须 50Hz，但 LED 调光建议 500Hz~1kHz 以上

**解决方案：**
把 LED 挪到另一个定时器，比如：
- TIM3_CH1 = PA6
- TIM4_CH1 = PB6

设置 LED 的定时器 ARR 和 PSC 产生 1kHz：
```c
PSC = 72-1   // 72MHz / 72 = 1MHz
ARR = 1000-1 // 1MHz / 1000 = 1kHz
```

**为什么不能让 TIM2 同时输出两个频率：**
同一个定时器的所有通道共用同一个 PSC 和 ARR（频率相同），只有 CCR（占空比）可以各自独立。

---

### 5. 中断里阻塞延时，主循环会卡住

**位置：** `Encoder.c:155、172`

**问题：**
```c
Delay_ms(20);  // 阻塞 20ms
while(GPIO_ReadInputDataBit(GPIOB, GPIO_Pin_3) == RESET) {}  // 可能阻塞几百毫秒
```

- 中断里阻塞期间，主循环停摆，OLED 不刷新、PWM 不更新
- 这是按键消抖的"不优雅但能用"写法

**更好的做法（状态机消抖）：**
- 中断只置标志位，不阻塞
- 主循环里用状态机处理消抖（读标志 → 延时 20ms → 再读引脚 → 确认有效）

**当前影响：**
按编码器按键时，OLED 会暂停刷新，LED 和舵机也会短暂停止响应。如果你觉得可以接受，不改也行。

---

### 6. 重复调用 NVIC_PriorityGroupConfig

**位置：** `Encoder.c:35` 和 `:74`、`KeyEXTI.c:39`

**问题：**
`NVIC_PriorityGroupConfig(NVIC_PriorityGroup_2)` 被调了多次，值一样所以无害，但不规范。

**修复方法：**
在 `main()` 开头调用一次：
```c
int main(void)
{
    NVIC_PriorityGroupConfig(NVIC_PriorityGroup_2);  // 全局设一次
    
    LED_Init();
    OLED_Init();
    // ...
}
```

然后删掉各个模块里的重复调用。

---

### 7. 注释残留

**位置：** `Encoder.c:151、176`

**问题：**
注释还写着"屏蔽 EXTI14 中断"，实际操作的是 `EXTI_Line3`（从 `KeyEXTI.c` 复制过来忘改了）。

**修复：**
把注释改成 `EXTI3`。

---

## 三、当前工程总结

### 功能状态
✅ 编码器调节 LED PWM 占空比（0~100%）  
✅ 编码器调节舵机角度（0~180°）  
✅ OLED 实时显示计数值和角度  
✅ 按键重置计数（有消抖）

### 已修复的问题
✅ `Servo.h` 函数名拼写错误（`SetAngel` → `SetAngle`）  
✅ `Servo_SetAngle()` 浮点计算强制类型转换

### 建议修复顺序
1. **必须改：** 加 `volatile`（第 1 项）
2. **建议改：** LED 占空比边界（第 2 项）
3. **可选改：** 其他优化项（第 4~7 项）

---

## 四、附：PWM 计算公式速查

```
PWM频率 Freq = CK_PSC / (PSC+1) / (ARR+1)
PWM占空比 Duty = CCR / (ARR+1)
PWM分辨率 Reso = 1 / (ARR+1)
```

**当前配置（TIM2）：**
- CK_PSC = 72MHz
- PSC = 71 → 分频后 1MHz
- ARR = 19999 → 频率 50Hz
- CCR 范围：0~19999

**舵机脉宽要求：**
- 0.5ms (500µs) → 0° → CCR=500
- 1.5ms (1500µs) → 90° → CCR=1500
- 2.5ms (2500µs) → 180° → CCR=2500
